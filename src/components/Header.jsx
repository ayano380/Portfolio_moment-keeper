import React from "react";

const Header = () => {

    return(
        <header>
            <h1>Moment Keeper</h1>
            <input type="search" placeholder="キーワードを入力" />
        </header>

    );
};

export default Header;